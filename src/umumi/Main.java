package umumi;

import java.util.ArrayList;
import java.util.List;
import java.util.Map;
import java.util.stream.Collectors;

public class Main {
    public static void main(String[] args) {
//        List<User> list = new ArrayList<>();
//        list.add(new User(1, "Vusala"));
//        list.add(new User(2, "Arzu"));
//        list.add(new User(3, "Sevinc"));
//        list.add(new User(4, "Kamil"));

        //list.stream().forEach(user -> user.toString());
        // list.stream().forEach(User::toString);
        //List<User> filteredlist= list.stream().filter(user -> user.id>2).collect(Collectors.toList());
        // System.out.println(filteredlist);
        // Long countedlist= list.stream().filter(user -> user.id>=2).count();
        // System.out.println(countedlist);
        //List<User> mapping = list.stream().map(user -> new User(user.id, "MR. " + user.name)).collect(Collectors.toList());
       // System.out.println(mapping);
       //Map<Integer,String> mapped= list.stream().collect(Collectors.toMap(user -> user.id, user -> user.name));
      //  mapped.forEach((key,value)-> System.out.println("key  "+key+"  value  "+value));
    }


}
